{
  "name": "fbcpos-desktop",
  "version": "1.0.0",
  "description": "FoodBaskets Corporation POS Windows PC Desktop App",
  "author": "Food Baskets Corporation",
  "main": "dist-electron/main.js",
  "scripts": {
    "build:web": "cd ../mobile && npx expo export -p web --output-dir ../desktop/dist",
    "build:electron": "tsc",
    "build": "npm run build:web && npm run build:electron && electron-builder --win",
    "pack:dir": "npm run build:web && npm run build:electron && electron-builder --dir",
    "pack:portable": "npm run build:web && npm run build:electron && electron-builder --win portable",
    "start": "npm run build:electron && electron .",
    "dev": "npm run build:web && npm run build:electron && electron ."
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^31.2.1",
    "electron-builder": "^24.13.3",
    "typescript": "^5.4.5",
    "@types/node": "^20.14.9"
  }
}